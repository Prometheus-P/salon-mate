# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# SalonMate .gitignore
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# ┌─────────────────────────────────────────────────────────────────────────────┐
# │ 🔒 환경 변수 및 시크릿 (SECURITY CRITICAL)                                    │
# └─────────────────────────────────────────────────────────────────────────────┘
# Environment files
.env
.env.*
!.env.example
!.env.*.example
.envrc

# Private keys and certificates
*.pem
*.key
*.crt
*.p12
*.pfx
*.jks
*.keystore
*.cer
*.der

# Secrets directories
secrets/
.secrets/
private/
credentials/

# Credential files
credentials.json
*credentials*.json
*secret*.json
*secrets*.json
service-account*.json
gcp-*.json
firebase-*.json
aws-*.json

# API keys and tokens
*.token
*-token.txt
api-keys.txt
api_keys.txt

# SSH keys
id_rsa*
id_dsa*
id_ed25519*
*.ppk

# GPG keys
*.gpg
*.asc
secring.*

# OAuth/Auth files
oauth*.json
auth*.json

# Backup files that may contain secrets
*.bak
*.backup
*.old

# ┌─────────────────────────────────────────────────────────────────────────────┐
# │ Node.js / Frontend                                                          │
# └─────────────────────────────────────────────────────────────────────────────┘
node_modules/
.pnpm-store/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
.npm
.yarn/cache
.yarn/unplugged
.yarn/build-state.yml
.yarn/install-state.gz

# Next.js
.next/
out/
.vercel

# Build outputs
dist/
build/
*.tsbuildinfo

# ┌─────────────────────────────────────────────────────────────────────────────┐
# │ Python / Backend                                                            │
# └─────────────────────────────────────────────────────────────────────────────┘
venv/
.venv/
env/
.env/
ENV/
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
pip-log.txt
pip-delete-this-directory.txt

# pytest
.pytest_cache/
.coverage
htmlcov/
.tox/
.nox/
coverage.xml
*.cover
*.py,cover
.hypothesis/

# mypy
.mypy_cache/
.dmypy.json
dmypy.json

# Celery
celerybeat-schedule
celerybeat.pid

# ┌─────────────────────────────────────────────────────────────────────────────┐
# │ 데이터베이스                                                                 │
# └─────────────────────────────────────────────────────────────────────────────┘
*.db
*.sqlite3
*.sqlite
*.sql.gz
pgdata/
data/postgres/
data/redis/

# ┌─────────────────────────────────────────────────────────────────────────────┐
# │ IDE / 에디터                                                                 │
# └─────────────────────────────────────────────────────────────────────────────┘
.idea/
.vscode/
*.swp
*.swo
*~
.project
.classpath
.c9/
*.launch
.settings/
*.sublime-workspace
*.sublime-project

# ┌─────────────────────────────────────────────────────────────────────────────┐
# │ 운영체제                                                                    │
# └─────────────────────────────────────────────────────────────────────────────┘
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
Desktop.ini

# ┌─────────────────────────────────────────────────────────────────────────────┐
# │ Docker                                                                      │
# └─────────────────────────────────────────────────────────────────────────────┘
docker-compose.override.yml
.docker/

# ┌─────────────────────────────────────────────────────────────────────────────┐
# │ 테스트 및 커버리지                                                           │
# └─────────────────────────────────────────────────────────────────────────────┘
coverage/
*.lcov
.nyc_output/
junit.xml
test-results/
playwright-report/
playwright/.cache/

# ┌─────────────────────────────────────────────────────────────────────────────┐
# │ 로그                                                                        │
# └─────────────────────────────────────────────────────────────────────────────┘
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# ┌─────────────────────────────────────────────────────────────────────────────┐
# │ 캐시                                                                        │
# └─────────────────────────────────────────────────────────────────────────────┘
.cache/
.parcel-cache/
.turbo/
.eslintcache
.stylelintcache
*.tsbuildinfo

# ┌─────────────────────────────────────────────────────────────────────────────┐
# │ 모바일 (Capacitor)                                                          │
# └─────────────────────────────────────────────────────────────────────────────┘
ios/
android/
!ios/.gitkeep
!android/.gitkeep

# ┌─────────────────────────────────────────────────────────────────────────────┐
# │ 🤖 AI / 개발 도구                                                            │
# └─────────────────────────────────────────────────────────────────────────────┘
# NOTE: .claude/, .specify/, CLAUDE.md 등 AI 설정 파일은 버전관리 대상입니다.
#       개발환경에 구애받지 않고 일관된 개발 경험을 위해 공유합니다.

# 로컬 전용 설정 (개인 권한/환경 설정)
.claude/settings.local.json

# Playwright MCP 브라우저 캐시
.playwright-mcp/

# ┌─────────────────────────────────────────────────────────────────────────────┐
# │ 기타                                                                        │
# └─────────────────────────────────────────────────────────────────────────────┘
*.tmp
*.temp
*.orig
*.swp
*.swo
.scannerwork/
.sonar/

# Local development notes (may contain sensitive info)
TODO.local.md
NOTES.local.md
*.local.md
scratch/
temp/

# Memory/History files that may contain sensitive context
.history/
.undo/
*.memory
*.context
