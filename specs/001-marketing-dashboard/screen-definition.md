# Marketing Dashboard - 화면 정의서

**Feature**: Marketing Dashboard
**Version**: 1.0
**Updated**: 2025-12-21
**Status**: Implementation Complete

---

## 1. 화면 개요

### 1.1 화면 목적
미용실 오너가 리뷰 성과, Instagram 포스팅 현황, 마케팅 지표를 한눈에 파악할 수 있는 종합 대시보드 화면.

### 1.2 접근 경로
- **URL**: `/dashboard`
- **Navigation**: 사이드바 "대시보드" 메뉴
- **권한**: 인증된 Shop Owner

### 1.3 화면 구조
```
┌─────────────────────────────────────────────────────────────┐
│ Sidebar (264px)  │  Main Content                           │
│ ┌─────────────┐  │  ┌─────────────────────────────────────┐ │
│ │ SalonMate   │  │  │ Header (Title + ShopSelector)      │ │
│ │ ───────────│  │  └─────────────────────────────────────┘ │
│ │ 대시보드   │  │                                          │
│ │ 리뷰 관리  │  │  ┌──────────────┐ ┌──────────────────┐  │
│ │ 인스타그램 │  │  │ ReviewStats  │ │ PendingReviews   │  │
│ │ 설정       │  │  │ (US1)        │ │ (US5)            │  │
│ └─────────────┘  │  └──────────────┘ └──────────────────┘  │
│                  │                                          │
│                  │  ┌─────────────────────────────────────┐ │
│                  │  │ PostingCalendar (US2)               │ │
│                  │  └─────────────────────────────────────┘ │
│                  │                                          │
│                  │  ┌──────────────┐ ┌──────────────────┐  │
│                  │  │ Engagement   │ │ TrendCharts      │  │
│                  │  │ Metrics(US3) │ │ (US4)            │  │
│                  │  └──────────────┘ └──────────────────┘  │
└─────────────────────────────────────────────────────────────┘
```

---

## 2. 컴포넌트 상세 정의

### 2.1 Header Section

#### 2.1.1 Title Area
| 속성 | 값 |
|------|-----|
| 제목 | "대시보드" |
| 부제 | "마케팅 현황을 한눈에 확인하세요" |
| Typography | Title: MUI Typography h4, Subtitle: subtitle1 |

#### 2.1.2 ShopSelector (FR-016)
| 속성 | 값 |
|------|-----|
| 위치 | 헤더 우측 |
| 레이블 | "워크스페이스" |
| 컴포넌트 | Radix Select + Popover |
| 기본값 | URL 파라미터 > localStorage > 첫번째 샵 |

**기능 명세**:
- 다중 샵 소유자를 위한 샵 전환 기능
- URL query parameter (`?shopId=xxx`)로 상태 유지
- localStorage에 마지막 선택 저장
- 샵 인사이트 Popover: 선택된 샵 정보, 연동 상태 뱃지

**상태**:
| 상태 | 표시 |
|------|------|
| Loading | Skeleton (h-10, w-48) |
| Error | "Failed to load shops" 텍스트 |
| Empty | "No shops found" 텍스트 |
| Data | Select dropdown + Insight button |

---

### 2.2 ReviewStats Card (US1: FR-001~FR-004)

#### 레이아웃
```
┌─────────────────────────────────────────────────────┐
│ Review Stats                          Updated 5m ago │
├─────────────────────────────────────────────────────┤
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌────────┐ │
│  │ 1,234    │ │ 4.8 ★★★★★│ │ 92.5%   │ │ 5      │ │
│  │ Total    │ │ Average  │ │ Response │ │ Pending│ │
│  │ Reviews  │ │ Rating   │ │ Rate    │ │(Orange)│ │
│  └──────────┘ └──────────┘ └──────────┘ └────────┘ │
├─────────────────────────────────────────────────────┤
│ By Platform                                          │
│ [Google] 800 reviews 4.7  [Naver] 434 reviews 4.9   │
└─────────────────────────────────────────────────────┘
```

#### 데이터 필드
| 필드 | 타입 | 설명 | FR |
|------|------|------|-----|
| total_reviews | number | 전체 리뷰 수 | FR-001 |
| average_rating | number (1.0-5.0) | 평균 평점 | FR-002 |
| response_rate | number (%) | 응답률 | FR-003 |
| pending_count | number | 미응답 리뷰 수 | FR-004 |
| by_platform | object | 플랫폼별 통계 | FR-004 |
| last_synced_at | ISO8601 | 마지막 동기화 시간 | FR-018 |

#### 상태별 UI
| 상태 | 표시 |
|------|------|
| Loading | CardSkeleton |
| Error | ErrorState + Retry 버튼 |
| Empty (no data) | EmptyState variant="reviews" |
| Empty (0 reviews) | 카드 + EmptyState 내부 |
| Data | 통계 그리드 |

---

### 2.3 PendingReviews Card (US5: FR-012~FR-014)

#### 레이아웃
```
┌─────────────────────────────────────────────────────┐
│ 답변 대기 리뷰                                   [5] │
│ 5개의 리뷰가 답변을 기다리고 있습니다                  │
├─────────────────────────────────────────────────────┤
│ ┌─────────────────────────────────────────────────┐ │
│ │ [A] 김철수  [Google]                          ▼ │ │
│ │     ★★★★★ 5  · 12월 15일                        │ │
│ ├─────────────────────────────────────────────────┤ │
│ │ (Expanded View)                                  │ │
│ │ ┌───────────────────────────────────────────┐   │ │
│ │ │ "서비스가 정말 좋았습니다. 다음에도..."      │   │ │
│ │ └───────────────────────────────────────────┘   │ │
│ │                                                  │ │
│ │ 답변                                             │ │
│ │ ┌───────────────────────────────────────────┐   │ │
│ │ │ (textarea)                                 │   │ │
│ │ └───────────────────────────────────────────┘   │ │
│ │                                                  │ │
│ │ [⚡ AI 답변 생성]  [✓ 게시]                       │ │
│ └─────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────┘
```

#### 인터랙션 흐름
1. **리뷰 카드 클릭** → 확장/축소 토글
2. **AI 답변 생성 클릭** (FR-013)
   - 버튼: "⚡ AI 답변 생성" → "생성 중..." (spinner)
   - 성공: textarea에 응답 표시, 편집 모드 활성화
   - 실패: 에러 메시지 표시
3. **게시 클릭** (FR-014)
   - Dialog 오픈: 답변 미리보기 + 확인/취소
   - 확인 → API 호출 → 성공 메시지
   - 리뷰가 목록에서 제거됨

#### 컴포넌트
- **PlatformBadge**: Google (파랑), Naver (녹색)
- **StarRating**: 1-5 별점 표시
- **Dialog**: 게시 확인 모달 (Radix Dialog)

#### 상태
| 상태 | 표시 |
|------|------|
| Loading | CardSkeleton |
| Error | ErrorState + Retry |
| Empty | EmptyPendingReviews (체크 아이콘 + "모든 리뷰에 답변 완료") |
| Data | 스크롤 가능한 리뷰 리스트 (max-h-96) |

---

### 2.4 PostingCalendar Card (US2: FR-005~FR-007)

#### 레이아웃
```
┌─────────────────────────────────────────────────────┐
│ Posting Calendar                      Updated 10m ago│
├─────────────────────────────────────────────────────┤
│ [<] 2025년 12월 [>] [오늘]         [Week] [Month]   │
├─────────────────────────────────────────────────────┤
│ Legend: ●게시됨  ●예약됨  ●실패                      │
├─────────────────────────────────────────────────────┤
│  일   월   화   수   목   금   토                    │
│ ┌───┐┌───┐┌───┐┌───┐┌───┐┌───┐┌───┐              │
│ │ 1 ││ 2 ││ 3 ││ 4 ││ 5 ││ 6 ││ 7 │              │
│ │●  ││   ││●● ││   ││●  ││   ││   │              │
│ └───┘└───┘└───┘└───┘└───┘└───┘└───┘              │
│ ...                                                 │
└─────────────────────────────────────────────────────┘
```

#### View Modes (FR-006)
| 모드 | 표시 |
|------|------|
| Week | 7일 칼럼, 상세 포스트 카드, 하단 포스트 리스트 |
| Month | 달력 그리드, 상태 도트 표시, +N 개수 |

#### Status Colors (FR-007)
| 상태 | 배경색 | 텍스트 | 도트 |
|------|--------|--------|------|
| published | `bg-status-published-container` | `text-status-published` | `#2D8A4B` |
| scheduled | `bg-status-scheduled-container` | `text-status-scheduled` | `#B58103` |
| failed | `bg-status-failed-container` | `text-status-failed` | `#D8402B` |
| draft | `bg-status-pending-container` | `text-status-pending` | `#6B7280` |

#### 인터랙션
- **이전/다음 네비게이션**: 주/월 단위 이동
- **오늘 버튼**: 현재 날짜로 이동
- **뷰 토글**: Week ↔ Month 전환
- **날짜 클릭**: (Week view) 해당 날짜 포스트 상세 표시

#### 데이터 구조
```typescript
interface CalendarEntry {
  date: string;  // YYYY-MM-DD
  posts: PostSummary[];
}

interface PostSummary {
  id: string;
  status: 'published' | 'scheduled' | 'failed' | 'draft';
  image_url?: string;
  caption_snippet?: string;
  scheduled_at?: string;
  published_at?: string;
}
```

---

### 2.5 EngagementMetrics Card (US3: FR-008~FR-009)

#### 레이아웃
```
┌─────────────────────────────────────────────────────┐
│ Engagement Metrics                    Updated 5m ago │
├─────────────────────────────────────────────────────┤
│  ┌──────────────┐ ┌──────────────┐ ┌──────────────┐ │
│  │ ❤️  12.5K    │ │ 💬  3.2K     │ │ 👁  45.8K    │ │
│  │ Total Likes  │ │ Total Comments│ │ Total Reach  │ │
│  └──────────────┘ └──────────────┘ └──────────────┘ │
├─────────────────────────────────────────────────────┤
│ Top Performing Posts                                 │
│ ┌─────────────────────────────────────────────────┐ │
│ │ ① [img] ❤️1.2K 💬89            Score: 2.5K    │ │
│ │ ② [img] ❤️980  💬67            Score: 1.8K    │ │
│ │ ③ [img] ❤️756  💬45            Score: 1.2K    │ │
│ └─────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────┘
```

#### 데이터 필드
| 필드 | 타입 | 설명 | FR |
|------|------|------|-----|
| total_likes | number | 총 좋아요 수 | FR-008 |
| total_comments | number | 총 댓글 수 | FR-008 |
| total_reach | number | 총 도달 수 | FR-008 |
| top_posts | TopPost[] | 인기 포스트 (최대 5개) | FR-009 |
| last_synced_at | ISO8601 | 마지막 동기화 | FR-018 |

#### 숫자 포맷
- 1,000 이상: `1.2K`
- 1,000,000 이상: `1.5M`

---

### 2.6 TrendCharts Card (US4: FR-010~FR-011)

#### 레이아웃
```
┌─────────────────────────────────────────────────────┐
│ Trend Charts                        [7일][30일][365일]│
├─────────────────────────────────────────────────────┤
│ 리뷰 수                                              │
│ ┌─────────────────────────────────────────────────┐ │
│ │     📈 (LineChart - Review Count)               │ │
│ │                                                  │ │
│ └─────────────────────────────────────────────────┘ │
├─────────────────────────────────────────────────────┤
│ 평점 & 응답률                                        │
│ ┌─────────────────────────────────────────────────┐ │
│ │     📉 (LineChart - Rating + Response Rate)     │ │
│ │     Left Y: Rating (0-5)                        │ │
│ │     Right Y: Response Rate (0-100%)             │ │
│ └─────────────────────────────────────────────────┘ │
├─────────────────────────────────────────────────────┤
│ ● 리뷰 수  ● 평균 평점  ● 응답률                      │
└─────────────────────────────────────────────────────┘
```

#### Period Options (FR-011)
| 선택 | 값 | 레이블 |
|------|-----|--------|
| week | 7일 데이터 | "7일" |
| month | 30일 데이터 | "30일" |
| year | 365일 데이터 | "365일" |

#### 차트 라이브러리
- **Recharts** 사용
- LineChart, ResponsiveContainer
- 커스텀 Tooltip

#### 데이터 구조
```typescript
interface TrendDataPoint {
  date: string;
  review_count: number;
  average_rating: number;
  response_rate: number;
}
```

---

## 3. 공통 컴포넌트

### 3.1 EmptyState (FR-017)

| Variant | 아이콘 | 제목 | 설명 | 액션 |
|---------|--------|------|------|------|
| reviews | 📊 | "No review data yet" | "Connect your review platforms..." | "Connect Platforms" → /settings/integrations |
| calendar | 📅 | "No posts scheduled" | "Create your first Instagram post..." | "Create Post" → /posts/new |
| engagement | 📈 | "No engagement data" | "Publish posts to start tracking..." | "View Calendar" → /dashboard?tab=calendar |
| trends | 📉 | "Not enough data yet" | "Check back in a few days..." | - |
| pending-reviews | 🎉 | "All caught up!" | "No reviews need responses..." | - |

### 3.2 CardSkeleton
- 애니메이션: `animate-pulse`
- 구조: 제목 바 + 큰 값 바 + 설명 바

### 3.3 ErrorState
- 배경: `bg-red-50`, 테두리: `border-red-200`
- 아이콘: ⚠️
- Retry 버튼 제공

### 3.4 FreshnessIndicator (FR-018)
| 시간 | 표시 | 스타일 |
|------|------|--------|
| < 1분 | "Just now" | gray |
| < 60분 | "Xm ago" | gray |
| 1-24시간 | "Xh ago" | orange (isStale) |
| > 24시간 | "Xd ago" | orange (isStale) |

---

## 4. 반응형 디자인

### 4.1 Breakpoints
| 화면 | 너비 | 레이아웃 |
|------|------|---------|
| Mobile | < 768px | 1열, 세로 스택 |
| Tablet | 768px - 1024px | 2열 그리드 |
| Desktop | > 1024px | 2열 그리드 (lg:6/6) |

### 4.2 Grid 배치
```
Mobile (xs):
[ReviewStats] [PendingReviews] [PostingCalendar] [Engagement] [TrendCharts]
(모두 100% 너비, 세로 스택)

Desktop (lg):
Row 1: [ReviewStats 50%] [PendingReviews 50%]
Row 2: [PostingCalendar 100%]
Row 3: [Engagement 50%] [TrendCharts 50%]
```

---

## 5. 디자인 시스템

### 5.1 Color System (M3 Clinical & Airy)

#### Brand Colors
| 토큰 | Light | Dark | 용도 |
|------|-------|------|------|
| `--brand-ink` | #111827 | #f9fafb | 헤더, 본문 텍스트 |
| `--brand-cloud` | #f5f6fa | #111827 | 배경 |
| `--brand-snow` | #ffffff | #1f2937 | 카드 배경 |
| `--brand-blush` | #ffcccc | #f7a9b8 | 강조, 하이라이트 |
| `--brand-outline` | #e5e7eb | #374151 | 테두리 |

#### Status Colors (WCAG AA Compliant)
| 상태 | 전경색 | 배경색 |
|------|--------|--------|
| Published | #2D8A4B | #DDF5E3 |
| Scheduled | #B58103 | #FFF2CC |
| Failed | #D8402B | #FFE1DC |
| Pending | #6B7280 | #F3F4F6 |

### 5.2 Typography
- Material UI Typography 사용
- h4: 대시보드 제목
- subtitle1: 부제목
- text-lg: 카드 제목
- text-sm: 레이블, 설명

### 5.3 Card Style
- Outlined 스타일 (그림자 없음)
- `rounded-lg border border-gray-200 bg-white`
- padding: `p-6`

### 5.4 Motion
- 호버/포커스: 150ms ease-out
- 로딩: animate-pulse, animate-spin
- `prefers-reduced-motion` 존중

---

## 6. API 연동

### 6.1 Hooks (TanStack Query)
| Hook | Endpoint | Refetch |
|------|----------|---------|
| `useUserShops` | GET /api/v1/shops | - |
| `useReviewStats` | GET /api/v1/dashboard/{shopId}/review-stats | 5분 |
| `usePendingReviews` | GET /api/v1/dashboard/{shopId}/pending-reviews | 5분 |
| `usePostingCalendar` | GET /api/v1/dashboard/{shopId}/calendar | 5분 |
| `useEngagementMetrics` | GET /api/v1/dashboard/{shopId}/engagement | 5분 |
| `useTrendData` | GET /api/v1/dashboard/{shopId}/trends | 5분 |
| `useGenerateAIResponse` | POST /api/v1/reviews/{id}/generate-ai | - |
| `usePublishResponse` | POST /api/v1/reviews/{id}/publish | - |

### 6.2 Caching Strategy
- staleTime: 5분
- 페이지 로드 시 자동 refetch
- 백그라운드 refetch 활성화

---

## 7. 접근성

### 7.1 Keyboard Navigation
- Tab으로 모든 인터랙티브 요소 접근 가능
- Enter로 확장/축소, 클릭 동작
- Escape로 Dialog 닫기

### 7.2 Screen Reader
- 카드에 적절한 heading 구조 (h2, h3)
- 상태 뱃지에 aria-label
- 로딩 상태 표시

### 7.3 Color Contrast
- WCAG AA 기준 충족 (4.5:1)
- Status colors는 배경과 함께 사용

---

## 8. 성능 요구사항

| 지표 | 목표값 | 관련 SC |
|------|--------|---------|
| 초기 로드 | < 3초 | SC-001 |
| 캘린더 렌더링 (100 posts) | < 2초 | SC-004 |
| AI 응답 생성 | < 10초 | SC-007 |
| 데이터 신선도 | < 1시간 | SC-002 |

---

## 9. 파일 구조

```
src/frontend/src/app/dashboard/
├── page.tsx                     # 메인 대시보드 페이지
├── layout.tsx                   # 사이드바 레이아웃
├── components/
│   ├── ShopSelector.tsx         # 샵 선택기 (FR-016)
│   ├── ReviewStats.tsx          # 리뷰 통계 (FR-001~004)
│   ├── PendingReviews.tsx       # 대기 리뷰 (FR-012~014)
│   ├── PostingCalendar.tsx      # 포스팅 캘린더 (FR-005~007)
│   ├── EngagementMetrics.tsx    # 인게이지먼트 (FR-008~009)
│   ├── TrendCharts.tsx          # 트렌드 차트 (FR-010~011)
│   └── EmptyState.tsx           # 공통 상태 컴포넌트 (FR-017~018)
└── hooks/
    └── useDashboard.ts          # TanStack Query hooks
```
